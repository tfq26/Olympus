# Olympus Unified Environment Example
# Copy to .env at repo root. Each service (Flask, Node MCP, Frontend Vite) will source from here.

# -----------------
# Common Settings
# -----------------
MODEL_API_KEY=replace-with-nvidia-api-key
AWS_ACCESS_KEY_ID=replace-with-aws-access-key
AWS_SECRET_ACCESS_KEY=replace-with-aws-secret-key
AWS_DEFAULT_REGION=us-east-1

# -----------------
# Flask Backend
# -----------------
FRONTEND_ORIGIN=http://localhost:5173
FLASK_PORT=5000

# -----------------
# Node MCP Client
# -----------------
FLASK_URL=http://localhost:5000
PERSIST_TERRAFORM=1
# Optional Docker image name override for Terraform MCP server
TERRAFORM_MCP_IMAGE=mcps-terraform:latest

# -----------------
# Frontend (Vite) Firebase Auth
# These will be consumed by Vite with VITE_ prefix.
# Create Frontend/.env that references these or copy them directly.
# -----------------
# Primary backend URL (Node MCP server)
VITE_NODE_URL=http://localhost:8080
VITE_NODE_WS_URL=ws://localhost:8080

# Optional: Direct Flask access (monitoring endpoints)
VITE_FLASK_URL=http://localhost:5000

VITE_FIREBASE_API_KEY=replace-with-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=yourproject.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=yourproject-id
VITE_FIREBASE_STORAGE_BUCKET=yourproject.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:abcdef123456

# WebSocket + HTTP URLs for frontend
VITE_NODE_URL=http://localhost:8080
VITE_NODE_WS_URL=ws://localhost:8080
VITE_FLASK_URL=http://localhost:5000
