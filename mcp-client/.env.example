# Node MCP client environment example
MODEL_API_KEY=
PERSIST_TERRAFORM=1
# URL Flask backend will use to reach Node tools (if needed)
NODE_MCP_URL=http://localhost:8080
# Optional override for persistent Terraform container name
TERRAFORM_CONTAINER_NAME=mcps-terraform
